<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.member">
	<resultMap type="memberDTO" id="memResult">
		<result property="id" column="id" />
		<result property="pwd" column="pwd" />
		<result property="name" column="name" />
		<result property="email" column="email" />
		<result property="gender" column="gender" />
		<result property="joinDate" column="joinDate" />
	</resultMap>
	
	
	
	<insert id="insertMember" parameterType="memberDTO">
		<![CDATA[
			insert into user_board values(#{id}, #{pwd}, #{email}, #{name}, #{gender}, sysdate)
		]]>
	</insert>
	
	<select id="selectById" parameterType="String" resultType="int">
		<![CDATA[
			select count(*) from user_board where id=#{id}
		]]>
	</select>
	
	<select id="selectByEmail" parameterType="String" resultType="boolean">
		<![CDATA[
			select decode(count(*),1,'false','true') from user_board where email=#{email}
		]]>
	</select>
	
	
</mapper>